services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      SESSION_SECRET: ${SESSION_SECRET:-}
    volumes:
      - portfolio_db:/app/database
      - uploads:/app/public/uploads

volumes:
  portfolio_db:
  uploads: {}
